function calcular_media( homework, quiz, task ){

    let averageResult = ( homework * 0.5 ) + ( quiz * 0.2 ) + ( task * 0.6 )
        return averageResult

        alert( "Average = " + average_calc( 10.0, 10.0, 10.0 ))

    document.getElementById("averageResult-value").innerText = averageResult

}